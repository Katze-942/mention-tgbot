import { MultilingualDialogues } from '../../../interfaces/MultilingualDialogues'

const dialogues: MultilingualDialogues = {
  EN: `Send #admin (@admin) in the group with the bot to call all admins or #all (@all) to call everyone.

Commands to configure:
@all command:
All: <code>/set_all_all</code>
Admins only: <code>/set_all_admin</code>
Disable: <code>/set_all_noone</code>

@admin command:
All: <code>/set_admin_all</code>
Admins only: <code>/set_admin_admin</code>
Disable: <code>/set_admin_noone</code>`
}

export default dialogues
